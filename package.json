{
  "name": "loan-repayment-calculator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-nortune-runtime.mjs",
    "preview": "vite preview",
    "test": "node --test",
    "verify:dist": "node scripts/verify-nortune-dist.mjs",
    "manifest:refresh": "node scripts/create-nortune-manifest.mjs",
    "qc": "npm run test && npm run build && npm run verify:dist"
  },
  "dependencies": {
    "vue": "3.5.40"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "6.0.8",
    "vite": "8.1.5"
  }
}
